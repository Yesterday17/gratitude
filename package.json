{
  "private": true,
  "name": "gratitude",
  "repository": "https://github.com/Yesterday17/gratitude",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "lerna run dev --stream",
    "dev:server": "lerna run start --stream",
    "cloc": "cloc --exclude-dir=node_modules,dist ."
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}